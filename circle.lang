fun main()
{
    var x = -1;
    while (x < 1)
    {
        var y = -1;

        while (y < 1)
        {
            if (2*x*x + y*y <= 0.5)
                set_pixel(x, y, 72);

            y = y + 0.05;
        }

        x = x + 0.05;
    }

    flush();
    ret 0;
}
